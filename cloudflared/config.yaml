# ~/.cloudflared/config.yaml

tunnel: be2f132d-1586-43ab-ac90-70dd6856e7cf   # ваш Tunnel ID
credentials-file: /home/username/.cloudflared/be2f132d-1586-43ab-ac90-70dd6856e7cf.json

ingress:
  - hostname: uncensoredone.chipillm.uk
    service: http://localhost:8001     # тот же порт FastAPI/Uvicorn

  # catch-all на 404, чтобы всё остальное не "провалилось"
  - service: http_status:404
